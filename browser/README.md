# Image Viewer

Веб-приложение для просмотра и управления коллекцией изображений с метаданными.

## Возможности

- 📁 **Навигация по папкам** - просмотр изображений в иерархической структуре папок
- 🔍 **Мощный поиск** - поиск по промптам, тегам, фильтрация по статусу (отмеченные/неотмеченные), поиск дубликатов
- 🏷️ **Управление тегами** - автоматическая генерация тегов с помощью WD14 Tagger, ручное редактирование
- ⭐ **Рейтинг** - оценка изображений от 1 до 5 звезд
- 🔖 **Закладки** - сохранение позиций просмотра для быстрого доступа
- ⭐ **Избранное** - копирование изображений в отдельную папку
- 📊 **Сортировка** - по дате, имени файла, промпту, рейтингу, тегам, размеру, хешу, случайная
- 🖼️ **Миниатюры** - автоматическое создание миниатюр в формате WebP
- 💾 **База данных** - SQLite для хранения метаданных с автосохранением
- 🎯 **Обработка изображений** - автоматическое извлечение промптов из PNG метаданных

## Установка

```bash
# Создать/обновить conda окружение
conda env create -f ../environment.yml
# или если окружение уже есть:
conda env update -f ../environment.yml --prune

# Активировать окружение
conda activate neural-networks
```

## Запуск

```bash
cd browser
python backend/app.py
```

Или через bat файл:
```bash
app.bat
```

Приложение будет доступно по адресу: http://127.0.0.1:5000

## Настройка

Настройте `config.json`:

```json
{
    "image_folder": "путь/к/папке/с/изображениями",
    "favorites_folder": "путь/к/папке/избранного",
    "thumbnail_size": 512,
    "items_per_page": 20,
    "allowed_extensions": [".png", ".jpg", ".jpeg", ".webp", ".webm"],
    "metadata_folder": ".metadata",
    "database_name": "metadata.db",
    "favorite_tag": "favorite",
    "thumbnail_extension": ".webp",
    "thumbnail_quality": 85,
    "auto_tag_enabled": true,
    "auto_tag_method": "wd14",
    "auto_tag_threshold": 0.3771
}
```

### Параметры конфигурации

- `image_folder` - путь к папке с изображениями (обязательно)
- `favorites_folder` - путь к папке для избранного (обязательно)
- `thumbnail_size` - размер миниатюр в пикселях (по умолчанию: 512)
- `items_per_page` - количество изображений на странице (по умолчанию: 20)
- `allowed_extensions` - список разрешенных расширений файлов
- `metadata_folder` - имя папки для хранения метаданных (по умолчанию: `.metadata`)
- `database_name` - имя файла базы данных (по умолчанию: `metadata.db`)
- `favorite_tag` - тег, добавляемый к избранным изображениям (по умолчанию: `favorite`)
- `thumbnail_extension` - расширение файлов миниатюр (по умолчанию: `.webp`)
- `thumbnail_quality` - качество миниатюр WebP от 1 до 100 (по умолчанию: 85)
- `auto_tag_enabled` - включить/выключить автоматическую генерацию тегов
- `auto_tag_method` - метод генерации тегов (по умолчанию: `wd14`)
- `auto_tag_threshold` - порог вероятности для включения тега (по умолчанию: 0.3771)

## Поиск

Приложение поддерживает расширенный поиск с различными фильтрами:

- `text` - поиск по промпту в текущей папке
- `g:text` - глобальный поиск по промпту во всех папках
- `t:tag1,tag2` - поиск по тегам в текущей папке
- `g:t:tag1,tag2` - глобальный поиск по тегам
- `u:` - только неотмеченные изображения
- `u:text` - неотмеченные с текстом в промпте
- `c:` - только отмеченные изображения
- `c:text` - отмеченные с текстом в промпте
- `dh:` - дубликаты по хешу файла
- `dp:` - дубликаты по промпту

## Сортировка

Доступны следующие варианты сортировки:
- `date` - по дате создания/изменения
- `filename` - по имени файла
- `prompt` - по промпту
- `rating` - по рейтингу
- `tags` - по тегам
- `size` - по размеру файла
- `hash` - по хешу файла
- `random` - случайная сортировка

Сортировка может быть по возрастанию (`asc`) или убыванию (`desc`).

## Автоматическая генерация тегов

Приложение автоматически генерирует теги для изображений при создании метаданных используя **WD14 Tagger** от SmilingWolf.

### WD14 Tagger

Специализированная модель [SmilingWolf/wd-v1-4-swinv2-tagger-v2](https://huggingface.co/SmilingWolf/wd-v1-4-swinv2-tagger-v2) для аниме/хентай, обученная на Danbooru. Автоматически загружается с Hugging Face при первом использовании.

### Настройка в config.json:

```json
{
    "auto_tag_enabled": true,
    "auto_tag_method": "wd14",
    "auto_tag_threshold": 0.3771
}
```

- `auto_tag_enabled` - включить/выключить автоматическую генерацию тегов
- `auto_tag_method` - метод генерации тегов (по умолчанию: `wd14`)
- `auto_tag_threshold` - порог вероятности для включения тега (по умолчанию 0.3771 - оптимальный согласно README модели)

Модель будет автоматически загружена при первом использовании (может занять некоторое время).

### Как это работает

1. При создании метаданных для изображения (при обработке новых изображений) автоматически запускается генерация тегов через WD14 Tagger, если включена автоматическая генерация
2. Модель анализирует само изображение и добавляет теги с вероятностью выше порога
3. Теги сохраняются в метаданных изображения и могут быть отредактированы вручную

## База данных

Приложение использует SQLite базу данных для хранения метаданных:
- База данных хранится в памяти для быстрого доступа
- Автоматическое сохранение на диск с задержкой (debounce) для оптимизации производительности
- Расположение: `{image_folder}/{metadata_folder}/{database_name}`
- Хранит: промпты, теги, рейтинг, статус проверки, хеши, пути к файлам и миниатюрам, закладки

## Миниатюры

- Автоматическое создание миниатюр в формате WebP
- Миниатюры сохраняются рядом с оригинальными изображениями
- Качество и размер настраиваются в `config.json`
